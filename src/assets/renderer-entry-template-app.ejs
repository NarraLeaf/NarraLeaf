import React from "react";
import {createRoot} from "react-dom/client";
import {default as App} from "<%= appPath %>";

const root = createRoot(document.getElementById("root") as HTMLElement);
root.render(<App />);
